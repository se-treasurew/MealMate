# 安全政策

## 支持版本

| 版本 | 安全更新 |
| --- | --- |
| v1.x | 支持 |
| v0.x 及更早版本 | 不支持 |

请先确认问题可在最新的 v1.x 版本复现。旧版本使用者应先升级并执行随版本提供的迁移步骤。

## 私密报告漏洞

请使用本仓库 GitHub 页面 **Security -> Advisories -> Report a vulnerability** 的私密漏洞报告功能。报告至少应包含：受影响版本、部署方式、复现步骤、预期与实际结果、影响评估，以及不含真实家庭数据的最小证明材料。

**不要创建公开 Issue、Discussion 或 Pull Request，也不要在公开日志中粘贴令牌、密码、数据库、历史密钥、上传图片或可直接利用的细节。** 如果 GitHub 私密漏洞报告入口不可用，请私下联系仓库所有者，请其开启该功能或提供加密通信渠道；在获得私密渠道前不要公开披露。

维护者会尽快确认收到报告、复核影响并协调修复与披露时间。该社区项目不承诺固定响应 SLA；请不要在修复发布前公开漏洞细节。

## 密钥与初始化密码

- `JWT_SECRET` 必须是每个部署独立生成的高熵随机值，不得使用示例值、复用其他服务密钥或提交到 Git。泄露后应立即更换；更换会使现有 JWT 失效。
- `ADMIN_INITIAL_PASSWORD` 必须在首次初始化时显式提供，只用于创建初始店长。店长首次登录后必须改密。直接运行初始化脚本时，已有店长后可移除该变量；Compose 发布配置仍把它作为启动门禁，因此应将首次使用的实际值替换为新的高熵秘密，并避免写入 Shell 历史或提交到 Git。
- Web Push 已停用；历史 VAPID 私钥仍属于敏感信息，应从服务器配置中移除并继续禁止提交到仓库。
- `.env`、`*.pem`、数据库文件、备份、运行日志和上传目录不得进入源码发布树。提交或发布前运行 `python scripts/check_release_tree.py --mode worktree`，CI 使用 HEAD 模式复查。
- `CORS_ORIGINS` 应只列出实际可信前端来源。生产环境不要保留不需要的本地开发来源，也不要使用 `*`。
- `VITE_API_BASE_URL` 默认同源；跨源部署时仅配置受信任的 HTTPS API 地址。

若任何密钥、令牌或初始化密码曾进入提交、构建产物、日志或公开渠道，应视为已泄露并立即轮换；仅从 Git 历史删除字符串不能恢复其机密性。

## 数据、备份与隐私

MealMate 是自托管软件。SQLite 数据库可能包含账号、昵称、订单与用餐信息，`backend/uploads/` 可能包含家庭照片或其他个人内容。部署者负责：

- 限制服务器、备份介质、日志和上传目录的访问权限；
- 使用 TLS、系统补丁、磁盘/备份加密及合理的数据保留策略；
- 同时备份 SQLite 数据库和上传目录，并定期验证可恢复性；
- 在升级、迁移、重置或清理前制作一致性备份；
- 遵守适用的隐私法规，处理家庭成员的数据访问与删除请求；
- 不把真实生产数据上传到公开 Issue、CI 构件或第三方测试服务。

仓库维护者不托管部署实例，也无法恢复部署者丢失的密钥、数据库或上传文件。

## 安全响应边界

本政策覆盖受支持 v1.x 源码中可复现的认证、授权、订单所有权、文件上传、依赖与默认部署配置问题。以下事项主要由部署者或上游供应商负责：

- 主机入侵、弱 SSH/云账号凭据、防火墙、域名和 TLS 配置；
- 未按文档保护的 `.env`、JWT/历史 VAPID 密钥或初始化密码；
- 未备份导致的数据丢失，以及对数据库或上传目录的错误权限配置；
- 浏览器、操作系统、反向代理、容器运行时和第三方依赖自身的漏洞。

即使问题位于上述边界，也欢迎通过私密漏洞报告提供证据；维护者会判断是否需要改进代码、默认配置或文档，但不能代替部署者进行主机事件响应。
