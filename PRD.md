# 饭饭之交（MealMate）v1.0 产品需求文档

**文档版本：** v1.0
**更新日期：** 2026-08-23
**产品形态：** 家庭私有部署的移动优先点餐 PWA

## 1. 产品目标

MealMate 为一个家庭或小型固定群体提供私有菜单与点餐流程：饭团浏览菜单、加入购物车并下单；饲养员维护菜品并处理订单；店长维护成员和系统配置。产品不提供公开注册、支付、外卖配送或多租户 SaaS 能力。

v1.0 的发布目标是：

1. 在手机浏览器和安装后的 PWA 中完成浏览、下单、处理订单的闭环。
2. 通过明确的角色权限、严格订单状态机和会话失效机制保护家庭数据。
3. 支持单机 SQLite 私有部署和 Docker Compose 部署。
4. 使用 80+ 项隔离端到端测试和发布树扫描作为发布门禁。

## 2. 技术基线

| 层级 | v1.0 选型 |
| --- | --- |
| 后端 | Python 3.13、FastAPI、Uvicorn |
| 数据访问 | SQLAlchemy 2.0 异步 ORM、aiosqlite、SQLite |
| 认证与媒体 | JWT、passlib/bcrypt、Pillow |
| 前端 | Node.js 22、Vue 3、TypeScript、Vite、Vant 4、Pinia、Vue Router |
| PWA | vite-plugin-pwa `injectManifest`、自定义 Service Worker、离线缓存与版本更新 |
| 部署 | Docker Compose、Nginx、Caddy，同源 API/上传反向代理 |

前端 `VITE_API_BASE_URL` 默认为空，即使用同源 `/api` 和 `/uploads`。本地 Vite 开发服务器通过代理连接 `localhost:8000`；该开发地址不得进入生产构建。后端 `CORS_ORIGINS` 使用逗号分隔的精确来源列表，生产部署必须替换本地默认来源。

## 3. 用户、角色与权限

### 3.1 身份定义

| 身份 | 核心权限 |
| --- | --- |
| 店长 | 唯一管理员；创建成员、启停账号、授予/收回饲养员权限、重置成员密码、修改角色显示名称；同时拥有饲养员权限 |
| 饲养员 | 管理分类、标签、菜品、图片和参考链接；查看全部订单；推进订单状态 |
| 饭团 | 浏览上架菜品、维护购物车、登录后下单、查看本人订单、取消本人待处理订单 |
| 游客 | 浏览上架菜品列表与详情、分类和标签；维护浏览器本地购物车 |

游客不能提交订单、查看订单或进入管理功能。未登录用户提交购物车时应进入登录流程，购物车在登录前后继续保留。

饲养员可在饭团视图和管理视图之间切换，但这是同一账号的界面模式，不是账号切换。v1.0 已移除免密多账号切换及长期账号令牌列表，旧版 `mealmate_accounts` 本地数据在启动时清理。

### 3.2 账号生命周期

- 不开放自助注册；店长创建普通成员并决定是否授予饲养员权限。
- 初始店长用户名为 `admin`，密码必须由部署者通过 `ADMIN_INITIAL_PASSWORD` 提供，不存在内置密码。
- 初始店长、店长新建的成员和被重置密码的成员首次登录后必须修改密码。
- v1.0 不提供成员删除 API；店长不能把其他账号提升为店长，也不能禁用或降级唯一店长。
- 禁用账号后登录被拒，已有令牌立即失效；重新启用不恢复旧令牌。

## 4. 功能需求

### 4.1 认证与个人资料

1. 用户名和密码登录成功后返回 Access Token、Refresh Token 与 `must_change_password`。
2. `POST /api/auth/refresh` 仅接受 JSON 请求体中的 `refresh_token`；查询参数调用返回校验错误。
3. Access Token 默认 120 分钟，Refresh Token 默认 7 天，均包含用户 `token_version`。
4. 修改密码校验旧密码；成功后递增 `token_version`，返回一组新令牌，旧 Access/Refresh Token 均失效。
5. 店长重置密码或禁用账号也递增 `token_version`；重新启用后，禁用前的旧令牌仍不可恢复。
6. `must_change_password=true` 时，前端只允许用户进入个人资料页完成改密。
7. 用户可修改昵称、选择内置头像或上传自定义头像。

### 4.2 菜单与菜品

1. 游客和饭团只能查看 `active` 菜品；饲养员和店长可查看全部状态。
2. 菜品包含名称、分类、Markdown 描述、备注、状态、自由标签、参考链接和多张图片。
3. 支持按分类、状态和关键词筛选；关键词可匹配菜名和标签。
4. 饲养员可创建、编辑、上下架和删除菜品，管理分类、标签、图片和链接。
5. 删除仍被菜品引用的分类必须失败；删除标签只移除关联。
6. Markdown 输出必须经过 DOMPurify 清理后再渲染。

### 4.3 图片与头像

1. 每个文件最大 5 MiB，仅接受扩展名为 `.jpg`、`.jpeg`、`.png`、`.webp` 的真实图片。
2. 菜品图片单次最多上传 5 张；生产反向代理必须在 multipart 解析前限制头像和菜品图片请求体总大小。
3. 后端必须解码图片并验证真实格式与扩展名匹配，拒绝损坏或伪装文件，失败时不得留下文件。
4. 接受的图片统一编码为 WebP，并生成最大 `200 x 200` 的 WebP 缩略图。
5. 自定义头像复用相同上传管线；资料更新只允许清空头像、使用固定 `/avatars/*.png` 白名单或保留本人当前上传路径。
6. 外部 URL、目录穿越、任意 `/uploads` URL 和他人的上传路径必须被拒绝。
6. `backend/uploads/` 是部署运行数据，不属于源码发布物。

### 4.4 购物车与订单

1. 购物车保存在浏览器 `localStorage`，无需登录即可加购、修改数量和备注。
2. 下单时必须登录，订单至少包含一个仍处于上架状态的菜品，并包含用餐日期与有效餐次。
3. 饭团只能查看自己的订单；饲养员和店长默认查看全部订单，也可筛选状态。
4. 订单状态只允许以下前向流转：

```text
pending -> accepted -> cooking -> done
```

5. 禁止跳级、回退、重复设置状态或从 `done`/`cancelled` 重开。
6. 只有订单本人可以把自己的 `pending` 订单改为 `cancelled`；饲养员和店长不能代取消，`accepted` 后不支持取消请求。
7. 新订单和状态变化通过订单页查看；系统不发送后台通知，家庭成员可直接沟通。

### 4.5 PWA

- Manifest 提供应用名称、主题色、192/512 图标、`standalone` 展示模式。
- Service Worker 使用 `injectManifest`，预缓存前端资源；菜单 API 使用 NetworkFirst，上传图片使用 CacheFirst。
- 前端由 `virtual:pwa-register` 统一注册 Service Worker；新版本在后台安装后显示更新提示，由用户确认刷新。入口文件、`sw.js`、Manifest 和图标禁止长期缓存，带内容哈希的 `/assets/` 文件使用长期缓存。
- Web Push 已停用。考虑到中国大陆网络下 FCM 可用性不可控，推送失败不得影响登录、菜单或订单等主业务；当前实现不连接任何推送服务，部署不需要 VAPID 或 FCM 网络。
- 旧版 `/api/push/*` 仅保留兼容响应，不保存新订阅、不读取历史订阅，也不发送通知。订单状态由用户在订单页查看。

## 5. API 契约摘要

| 模块 | 主要路径 | 访问范围 |
| --- | --- | --- |
| 健康检查 | `GET /health` | 公开 |
| 认证 | `/api/auth/login`、`/refresh`、`/me`、`/profile`、`/password`、`/avatar` | 登录/刷新按端点要求 |
| 成员 | `/api/users` | 店长 |
| 分类 | `/api/categories` | 读取公开，写入为饲养员/店长 |
| 标签 | `/api/tags` | 读取公开，写入为饲养员/店长 |
| 菜品 | `/api/dishes` | 上架内容公开，管理为饲养员/店长 |
| 订单 | `/api/orders` | 登录用户，结果和操作受角色/所有权约束 |
| 推送兼容 | `/api/push` | 仅供旧版 PWA 获取停用状态，不保存订阅或发送通知 |
| 配置 | `/api/config` | 读取公开，修改为店长 |

所有受保护端点使用 `Authorization: Bearer <access_token>`。认证失败返回 401，角色或所有权不足返回 403，资源不存在返回 404，业务状态冲突或输入不合法返回 400/422。

## 6. 数据、初始化与迁移

### 6.1 核心数据

- `User`：账号、密码哈希、昵称、头像、角色、启用状态、强制改密标记、`token_version`。
- `DishCategory`、`Dish`、`Tag`：分类、菜品、自由标签及多对多关系。
- `DishImage`、`DishLink`：菜品图片、缩略图和参考链接。
- `Order`、`OrderItem`：订单、状态、餐次、备注和明细。
- `PushSubscription`：历史兼容表，停用后不再写入。
- `SystemConfig`：店长/饲养员/饭团显示名称。

### 6.2 初始化与升级

- 新部署运行 `python -m app.init_db`，幂等创建表并写入初始店长、6 个预设分类、预设标签和角色显示名称。
- `ADMIN_INITIAL_PASSWORD` 在初始化时必填且至少 6 位；创建的店长必须首次改密。
- 现有部署先备份数据库与上传数据，再运行 `python -m app.migrate`。
- 当前迁移脚本负责创建缺失表、补充 `is_active`、`must_change_password`、`token_version` 并迁移旧预设头像路径；它不是通用 Alembic 历史，复杂表结构变更需要专用迁移。

## 7. 部署与配置要求

### 7.1 必需配置

- `JWT_SECRET`：每个部署独立的高熵随机密钥。
- `ADMIN_INITIAL_PASSWORD`：初始化店长使用的临时密码，首次登录后更换。
- `CORS_ORIGINS`：跨源部署时设置为实际前端来源；同源生产部署不应继续保留本地开发来源。

### 7.2 可选配置

- `DATABASE_URL`、Token 有效期、`UPLOAD_DIR`。
- `VITE_API_BASE_URL`：仅跨源前端构建需要；默认同源。

Docker Compose 提供 backend、frontend、caddy 三个服务。发布验证必须执行 `docker compose config`，并显式提供 `JWT_SECRET` 和 `ADMIN_INITIAL_PASSWORD` 测试值；还必须分别构建 backend/frontend 镜像，但无需启动真实生产服务。

## 8. 安全与隐私要求

1. `.env`、PEM、SQLite 文件、上传目录、依赖目录、构建目录和本地工具目录不得进入发布树。
2. 密码只存储 bcrypt 哈希；部署者负责选择足够强的初始化密码和轮换密钥。
3. SQL 查询通过 SQLAlchemy 参数化表达式完成。
4. CORS 不允许生产环境使用任意来源通配符。
5. SQLite 与上传图片可能包含家庭成员信息，访问控制、磁盘加密、备份、保留周期和删除请求由部署者负责。
6. 安全漏洞必须通过 GitHub 私密漏洞报告功能提交，不得公开披露未修复细节。

详见 [SECURITY.md](SECURITY.md)。

## 9. 发布验收标准

### 9.1 自动化门禁

- Python 3.13：后端单元测试通过。
- 90+ 项隔离 E2E 通过，不复用开发数据库、上传目录或固定服务端口。
- Node.js 22：`npm ci` 与 `npm run build` 通过。
- `npm audit --omit=dev` 与 `pip-audit` 通过。
- `docker compose config` 通过；backend/frontend Docker 镜像构建通过。
- 发布扫描器的 HEAD 模式通过，禁止敏感文件和移除路径进入发布树。
- `frontend/dist` 不包含 `localhost:8000`。

### 9.2 功能验收

- 游客只可浏览上架内容和使用本地购物车。
- 三种角色的读取与写入权限符合矩阵。
- 刷新令牌只接受 JSON；改密返回新令牌，旧令牌因 `token_version` 失效。
- 初始店长密码无默认值且首次改密。
- 订单只能严格前进，本人只可取消 `pending` 订单。
- 非法、损坏、伪装图片和非法头像路径均被拒绝且不落盘。
- 单账号会话正常；旧多账号列表被清理且不存在免密切换入口。

## 10. v1.0 范围外

- 公开注册、社交登录、支付、配送、库存与采购。
- 多租户和云端托管服务。
- `accepted` 后的取消申请或人工审批流程。
- 免密多账号切换。
- 通用数据库迁移平台和自动备份服务。
- WebSocket 实时同步；v1.0 使用页面刷新获取最新状态。

## 11. 发布与运维责任

MealMate 以自托管软件发布。项目维护者负责源码中的安全修复和发布说明；部署者负责服务器、TLS、域名、访问边界、密钥、数据库和上传数据的备份、隐私与恢复演练。真实数据不得用于公开 Issue 或测试附件。
